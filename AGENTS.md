> Hey we're making a new CLI in @cli/ using @cli-framework/ (which we adapt) to our own needs. The old cli is also in the repo fyi. See @plan-cli.md . go step by
step. command. test. make it NICE, how could it be elegant. clean lean

Use @radio4000/sdk to crud most channels and tracks. Fallback to sdk.supabase for more advanced things.

This is a CLI. Test it, use it. Run `bun run check`.

To test, I can recommend testing `ko002` (lots of descriptions, tags, discogs_url) or `oskar` (lotsa tracks) or `detecteve` (v1 radio).
